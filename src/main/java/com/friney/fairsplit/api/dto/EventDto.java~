package com.friney.fairsplit.api.dto;

import com.friney.fairsplit.core.entity.EventMember;
import com.friney.fairsplit.core.entity.Receipt;

import java.util.List;

public record EventDto(
        Long id,
                String name,
                String description,
                        List<EventMember> eventMembers,
                        List<Receipt> receipts
) {
}
